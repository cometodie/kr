{% extends 'base.html.twig' %}

{% block body %}
<div class="container margin_top">
    <div class="margin_top"></div>
    <h1>Изменение мероприятия</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" class="button success" value="Изменить" />
    {{ form_end(edit_form) }}

    <button class="button info"><a href="{{ path('event_index') }}">Назад в список</a></button>

    {{ form_start(delete_form) }}
        <input type="submit" class="button alert" value="Удалить">
    {{ form_end(delete_form) }}

</div>
{% endblock %}
